package cc.thevar.blukit.data.local.entities

import org.junit.Test

/**
 * Mappers for MessageEntity were removed as we now use MessagePayload directly.
 * Contact and Peer entities use simple serialization.
 */
class MappersTest {
    @Test
    fun `placeholder test`() {
        // Mappers fixed by removal of MessageEntity
    }
}
